services:
  - type: web
    name: arun-traders-billing
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node electron/server.js
    envVars:
      - key: NODE_ENV
        value: production